# Error N015: Duplicate Annotation

This error occurs when an annotation that has single cardinality appears more
than once on the same declaration. Arandu does not merge or silently discard
duplicate annotations.

```arandu
@NoFallback
@NoFallback
func critical() {}
```

Keep one occurrence:

```arandu
@NoFallback
func critical() {}
```
