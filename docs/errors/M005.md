# Error M005: Filesystem Import Forbidden

A quoted filesystem import was used while compiling a package. Package imports
must not smuggle host paths, versions, URLs, or undeclared source origins into
the module graph.

Declare the package under `[dependencies]` in `arandu.toml`, then import it by
the declared dependency alias. Quoted imports remain available only to the
legacy single-file command mode.
