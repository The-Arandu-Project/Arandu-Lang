# Error N013: Invalid Annotation Target

This error occurs when a known annotation is attached to a declaration kind it
does not support. Annotation targets are explicit and are never propagated to
related declarations implicitly.

```arandu
@NoFallback
struct Packet {}
```

Apply the annotation to one of its documented targets:

```arandu
@NoFallback
func critical() {}
```
