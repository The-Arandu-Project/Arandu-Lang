# Warning M004: Implicit Local Import

A package-mode source file used a bare local import such as `import util`.
This spelling is deprecated because it becomes ambiguous when a dependency has
the same alias.

Use the explicit package-local root instead:

```aru
import self.util as util
```

The diagnostic includes a structured replacement that editors can apply.
