# Error N014: Invalid Annotation Arguments

This error occurs when an annotation receives the wrong number or shape of
arguments. Each built-in annotation has a fixed, statically validated argument
contract.

```arandu
@NoFallback("heap")
func critical() {}
```

Use the annotation's documented argument form:

```arandu
@NoFallback
func critical() {}
```
