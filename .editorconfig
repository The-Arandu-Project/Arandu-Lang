root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true

[*.{rs,toml,json,yml,yaml,aru,sh,ps1,ts,js}]
trim_trailing_whitespace = true

[*.md]
# Two trailing spaces are a deliberate hard line break in Markdown.
trim_trailing_whitespace = false
